using System; 

class URI {

    static void Main(string[] args) { 

        int dist, tempo;

            dist = int.Parse(Console.ReadLine());

            tempo = dist * 2;

            Console.WriteLine(tempo + " minutos");

    }

}
